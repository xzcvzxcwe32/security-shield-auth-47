function processData(d) { return d.map(x => x * 2); }

// Update twnx
export class SystemEngine_twnx {
  private state: number = 2486;
  constructor() { console.log("Init twnx"); }
}

// Update fjl5
function processData(d) { return d.map(x => x * 2); }